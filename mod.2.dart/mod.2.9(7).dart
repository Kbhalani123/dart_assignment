//Write a program to print the number in reverse order.

void main() {
  int start = 10; // Starting number
  int end = 1;   // Ending number

  for (int i = start; i >= end; i--) {
  print(i);
  }
}